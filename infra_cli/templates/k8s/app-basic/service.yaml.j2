apiVersion: v1
kind: Service
metadata:
  name: {{ name }}
  namespace: {{ namespace | default("default") }}
spec:
  type: ClusterIP
  selector: { app: {{ name }} }
  ports:
    - port: 80
      targetPort: {{ port | default(8080) }}
      protocol: TCP
